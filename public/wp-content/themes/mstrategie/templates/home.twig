{% extends "layouts/base.twig" %}

{% block content %}
	<div class="o-container">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">

                {# <div class="hero">

                    <picture>
                        <source srcset="{{ post.thumbnail.src|towebp|resize(800) }}" type="image/webp">
                        <source srcset="{{ post.thumbnail.src|tojpg|resize(800) }}" type="image/jpeg">
                        <img src="{{ post.thumbnail.src|tojpg }}" alt="{{ post.title }}">
                    </picture>

                    <h1 class="c-heading -h1">{{post.title}}</h1>

                </div> #}

				
				<div class="article-body">
					
					{{post.content}}
					
				</div>


			</section>
		</article>
	</div>
{% endblock %}